'use strict';

/**
 * enterprise-left-body router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::enterprise-left-body.enterprise-left-body');
