'use strict';

/**
 * enterprise-left-body controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::enterprise-left-body.enterprise-left-body');
